# Design Clone Skill - Python Dependencies
# Install: pip install -r requirements.txt

# No external Python dependencies required
# Figma scripts use only stdlib modules (urllib, json, argparse)
# AI analysis now uses Claude Code built-in vision (no API needed)
